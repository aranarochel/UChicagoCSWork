signature DERIVATION = sig
 
  val draw : string * (string option) -> unit
  (* draw consumes source code (a string), and 
   * an optional filename to which to write the 
   * derivation illustration
   *)

end
